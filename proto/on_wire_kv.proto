syntax = "proto3";

package frenzykv;

message on_wire_kv
{
    uint32 sequence_number = 1;
    bytes user_key = 2;

    // Empty user value means this on_wire_kv is a tombstone,
    // Server will never return a uninited user_value.
    optional bytes user_value = 3;
}
